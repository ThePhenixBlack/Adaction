// Configuration runtime pour l'API
// Par défaut : localhost (pratique en dev). Sur Vercel tu peux définir
// une variable `window.API_BASE` via un petit script injecté ou remplacer
// cette valeur côté serveur si nécessaire.
window.API_BASE = window.API_BASE || 'http://localhost:3000';
